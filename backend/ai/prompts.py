"""AI提示词模板 - 所有用于生成总结和标签的提示词"""

# 单段总结提示词
SUMMARY_PROMPT = """你是一位专业的小说内容分析师。请阅读以下小说文本，生成一段简洁准确的内容总结。

要求：
1. 总结长度控制在{length}字左右
2. 覆盖核心主线剧情、主要人物关系
3. 内容客观，不添加主观评价
4. 语言简洁连贯，无冗余信息

小说文本：
{text}

请直接输出总结内容："""

# 分段总结合并提示词
CHUNK_MERGE_SUMMARY_PROMPT = """你是一位专业的小说内容分析师。以下是一部长篇小说各段落的分段总结，请将它们合并为一段完整、连贯的总结。

要求：
1. 最终总结长度控制在{length}字左右
2. 保留核心主线剧情，去除重复信息
3. 确保总结逻辑连贯，时间线清晰
4. 突出最重要的人物和情节

各段总结如下：
{chunk_summaries}

请输出合并后的完整总结："""

# 题材标签提示词
TAG_GENRE_PROMPT = """你是一位小说分类专家。请根据以下小说文本，判断其题材类型。

参考标签库中的题材标签：{tag_library}

要求：
1. 从参考标签中选择最贴合的1-3个题材标签
2. 如果参考标签中没有完全匹配的，可以使用其他合适的题材标签
3. 标签之间用逗号分隔

小说文本：
{text}

请直接输出题材标签（逗号分隔）："""

# 风格标签提示词
TAG_STYLE_PROMPT = """你是一位小说风格鉴赏家。请根据以下小说文本，判断其写作风格。

参考标签库中的风格标签：{tag_library}

要求：
1. 从参考标签中选择最贴合的1-3个风格标签
2. 如果参考标签中没有完全匹配的，可以使用其他合适的风格标签
3. 标签之间用逗号分隔

小说文本：
{text}

请直接输出风格标签（逗号分隔）："""

# 核心元素标签提示词
TAG_ELEMENT_PROMPT = """你是一位小说元素分析师。请根据以下小说文本，提取其核心元素。

参考标签库中的元素标签：{tag_library}

要求：
1. 从参考标签中选择最贴合的1-3个核心元素标签
2. 如果参考标签中没有完全匹配的，可以使用其他合适的元素标签
3. 标签之间用逗号分隔

小说文本：
{text}

请直接输出核心元素标签（逗号分隔）："""

# 人物类型标签提示词
TAG_CHARACTER_PROMPT = """你是一位小说人物分析专家。请根据以下小说文本，识别其中的主要人物类型。

参考标签库中的人物标签：{tag_library}

要求：
1. 从参考标签中选择最贴合的1-3个人物类型标签
2. 如果参考标签中没有完全匹配的，可以使用其他合适的人物类型标签
3. 标签之间用逗号分隔

小说文本：
{text}

请直接输出人物类型标签（逗号分隔）："""

# 专属标签提示词
TAG_EXCLUSIVE_PROMPT = """你是一位创意标签设计师。请根据以下小说文本，为这部小说创造1-2个专属标签。

要求：
1. 专属标签应贴合文本独特内容，突出核心特色
2. 每个标签控制在4个字以内
3. 不要使用通用标签，要体现这部小说的独特性
4. 标签之间用逗号分隔

示例：民国旧梦、星际救赎、权谋暗涌、仙途迷踪

小说文本：
{text}

请直接输出1-2个专属标签（逗号分隔）："""

# 标签维度到提示词的映射
DIMENSION_PROMPT_MAP = {
    "genre": TAG_GENRE_PROMPT,
    "style": TAG_STYLE_PROMPT,
    "element": TAG_ELEMENT_PROMPT,
    "character": TAG_CHARACTER_PROMPT,
    "exclusive": TAG_EXCLUSIVE_PROMPT,
}
