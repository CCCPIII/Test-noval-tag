"""AI提示词模板 - 所有用于生成总结和标签的提示词"""

# 单段总结提示词
SUMMARY_PROMPT = """你是一位专业的小说内容分析师。请阅读以下小说文本，生成一段简洁准确的内容总结。

要求：
1. 总结长度控制在{length}字左右
2. 覆盖核心主线剧情、主要人物关系
3. 内容客观，不添加主观评价
4. 语言简洁连贯，无冗余信息

小说文本：
{text}

请直接输出总结内容："""

# 分段总结合并提示词
CHUNK_MERGE_SUMMARY_PROMPT = """你是一位专业的小说内容分析师。以下是一部长篇小说各段落的分段总结，请将它们合并为一段完整、连贯的总结。

要求：
1. 最终总结长度控制在{length}字左右
2. 保留核心主线剧情，去除重复信息
3. 确保总结逻辑连贯，时间线清晰
4. 突出最重要的人物和情节

各段总结如下：
{chunk_summaries}

请输出合并后的完整总结："""

# 题材标签提示词
TAG_GENRE_PROMPT = """你是一位小说分类专家。请根据以下小说文本，判断其题材类型。

参考标签库中的题材标签：{tag_library}

要求：
1. 从参考标签中选择最贴合的1-3个题材标签
2. 如果参考标签中没有完全匹配的，可以使用其他合适的题材标签
3. 标签之间用逗号分隔

小说文本：
{text}

请直接输出题材标签（逗号分隔）："""

# 风格标签提示词
TAG_STYLE_PROMPT = """你是一位小说风格鉴赏家。请根据以下小说文本，判断其写作风格。

参考标签库中的风格标签：{tag_library}

要求：
1. 从参考标签中选择最贴合的1-3个风格标签
2. 如果参考标签中没有完全匹配的，可以使用其他合适的风格标签
3. 标签之间用逗号分隔

小说文本：
{text}

请直接输出风格标签（逗号分隔）："""

# 核心元素标签提示词
TAG_ELEMENT_PROMPT = """你是一位小说元素分析师。请根据以下小说文本，提取其核心元素。

参考标签库中的元素标签：{tag_library}

要求：
1. 从参考标签中选择最贴合的1-3个核心元素标签
2. 如果参考标签中没有完全匹配的，可以使用其他合适的元素标签
3. 标签之间用逗号分隔

小说文本：
{text}

请直接输出核心元素标签（逗号分隔）："""

# 人物类型标签提示词
TAG_CHARACTER_PROMPT = """你是一位小说人物分析专家。请根据以下小说文本，识别其中的主要人物类型。

参考标签库中的人物标签：{tag_library}

要求：
1. 从参考标签中选择最贴合的1-3个人物类型标签
2. 如果参考标签中没有完全匹配的，可以使用其他合适的人物类型标签
3. 标签之间用逗号分隔

小说文本：
{text}

请直接输出人物类型标签（逗号分隔）："""

# 专属标签提示词
TAG_EXCLUSIVE_PROMPT = """你是一位创意标签设计师。请根据以下小说文本，为这部小说创造1-2个专属标签。

要求：
1. 专属标签应贴合文本独特内容，突出核心特色
2. 每个标签控制在4个字以内
3. 不要使用通用标签，要体现这部小说的独特性
4. 标签之间用逗号分隔

示例：民国旧梦、星际救赎、权谋暗涌、仙途迷踪

小说文本：
{text}

请直接输出1-2个专属标签（逗号分隔）："""

# 标签维度到提示词的映射
DIMENSION_PROMPT_MAP = {
    "genre": TAG_GENRE_PROMPT,
    "style": TAG_STYLE_PROMPT,
    "element": TAG_ELEMENT_PROMPT,
    "character": TAG_CHARACTER_PROMPT,
    "exclusive": TAG_EXCLUSIVE_PROMPT,
}

# 一次性生成所有维度标签的合并提示词（减少 API 调用次数）
TAG_ALL_DIMENSIONS_PROMPT = """你是一位专业的小说分类与分析专家。请根据以下小说文本，从5个维度分析并生成标签。

【重要规则】
- 优先使用标签库中已有的标签，标签库中没有合适的才可以自拟
- 自拟标签必须是读者看到就能理解的、有信息量的词语，不要生成模糊或过于宽泛的描述性短语
- 所有维度的标签不能重复（同一个标签名不能出现在两个维度中）
- 好的标签示例：玄幻、扮猪吃虎、神雕侠侣、黄蓉、修罗场
- 坏的标签示例：废柴逆袭主角、热血爽文类、东方玄幻修真风格

标签库（优先从中选择）：
{tag_library}

维度说明：
- genre（题材）：小说类型分类，如玄幻、都市、言情、同人等。如果是同人/二创作品，必须包含"同人"
- style（风格）：写作风格特征，如爽文、慢热、硬核、扮猪吃虎等
- element（元素）：核心情节元素和涉及的原作品名。例如：系统、重生、修罗场。如果是同人作品，必须在此维度标注涉及的具体原作品名（如神雕侠侣、火影忍者、鲁路修等）
- character（人物）：主要角色的类型或具体姓名。可以是角色类型标签（如废柴、腹黑），也可以是小说中的重要出场人物姓名（如黄蓉、鲁路修、林动等）
- exclusive（专属）：本作独有的特色标签，体现这部小说的独特性

要求：
1. 每个维度选择1-3个最贴合的标签
2. 同一个标签名只能出现在一个维度中，绝对不能重复
3. 标签应简洁有信息量，一般2-6个字
4. 严格按照 JSON 格式输出，不要输出其他内容

输出格式：
{{"genre": ["标签1", "标签2"], "style": ["标签1", "标签2"], "element": ["标签1", "标签2", "标签3"], "character": ["标签1", "标签2"], "exclusive": ["标签1", "标签2"]}}

小说文本：
{text}

请直接输出 JSON："""
